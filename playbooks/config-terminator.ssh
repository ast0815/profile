---
- hosts: localhost
  become: yes
  tasks:
    - apt:
        name: fonts-inconsolata
    - apt:
        name: terminator

- hosts: localhost
  become: no
  tasks:
    - copy:
        src: terminator-config.txt
        dest: ~/.config/terminator/config
